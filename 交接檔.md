# 需求軌跡監控系統 — 交接檔

> 建置：2026-08-08～08-09（DT）｜最後更新：**2026-08-13 晚**（移除交易洽談＋修沙箱寫入）
> 狀態：**全自動運轉中**｜本檔為完整交接，新 session 讀此檔即可接手
> ⚠️ 2026-08-13 重點：卡片分類改為 3 類（重點經營/長期追蹤/封存），
> **交易洽談功能已整個移除**；登入版讀取與寫入的沙箱卡死都已修復（見第十節）。

## 〇、新對話開場提示詞（新 session 直接複製這段）

```
需求軌跡監控系統。請先讀交接檔再接手：
C:\Claude\projects\demand-trace\交接檔.md（本機記憶 demand-trace-system.md 也會自動載入）。
※ 路徑已於 08-14 從 docs\ 移到 repo 根目錄——docs\ 會被 GitHub Pages 公開送出。

【現況 2026-08-14】
- 公開版(github.io) ＋ 登入版(Apps Script) 並存運轉，三個排程照跑。
- 登入版=主力：https://script.google.com/macros/s/AKfycbwW2uvay_YEVMi62IuWebnKOKwI7uAMWIZ0Gzi-imiq2lsWAvht9jgxlvdBtQX5plY/exec
  白名單：mac2good909777、yubob0513(助理)、nibreeze(同助理)。登入部署 v16。
- 分類＝3 類：重點經營／長期追蹤／封存（交易洽談已移除，owner-only 機制一併拿掉）。
- 剛完成(08-13晚)：修好登入版「所有寫入互動靜默卡死」(沙箱內改走 google.script.run)、
  移除交易洽談、四家公司的經營斡旋資訊清理。
- 剛完成(08-14)：新增「🏢公司集團」與「🔗產業鏈」兩個分頁——
  watchlist.json 新增 group 欄(已填11家股東組成，如大統益←統一37%+大成長城27%)，
  公司軌跡卡片顯示≤3個🏢集團標籤；產業鏈7條(上中下游+🎯配對線)內嵌在
  index.html 的 /*CHAIN-DATA-START*/…END 區塊(刻意不做獨立json，避沙箱fetch)。
  另 epGet 加8秒逾時，公開版不再卡「載入中」。詳見第十二節。
- 公司集團／產業鏈＝**一次性建置，半年更新一次**（非每日排程維護）：
  最後更新 2026-08-14，下次建議 2027-02-14；頁面標題會自動顯示。
  ⚠️ 每日排程 prompt 需拿掉「深研輪替補查股東組成寫 group」那段。

【待辦】
- 公開版移除範圍未定：A 導引頁替換｜B repo 轉 private＋doGet 改讀 GitHub API｜C 暫不移除。
- 登入版首屏約 18 秒可優化（伺服端 4 次 UrlFetch）。

【別踩雷】
- 排程勿自動寫 watchlist 的 track/trackWhy(長期追蹤只人工加)。
- 登入版沙箱內不得出現任何 client 端 fetch(EP) 或相對路徑 fetch，一律走 epGet/epPost。
- 未確認登入版正常前勿關 GitHub Pages(登入版靠它供殼+資料)。
- 改 .gs 要用工具列磁碟鈕存檔 → 管理部署作業 → 登入部署(AKfycbwW2uvay) → 建立新版本 → 部署。
- 公開版 github.io 是全世界可讀：任何我方經營／斡旋內容都不該寫進 **docs/ 底下任何檔案**
  （docs/ 即 Pages 根目錄，.md 也會被原樣送出——本檔 08-14 前就是這樣被公開的）。
```

## 一、專案目標

追蹤中部（台中/彰化/南投/苗栗/雲林/嘉義）公司的經營軌跡——**購置、處分、產業鏈動態**——
交叉判讀真實意圖，在需求成形的最早時點切入：處分＝幫它賣、購置＝幫它找、產業鏈＝追交易對手。
Claude 角色＝監控員＋經營協作教練（每天回看 Mac 的經營進度並給下一步）。

## 二、核心方法論（Mac 定調，勿改）

1. **訊號時效權重：重訊 ＞ 新聞 ＞＞ 實價登錄**（同一事件以最早訊號源起算時效）
2. **時間軸模型**：重訊≈私契簽約 → 交廠 4個月~1年 → 公契(謄本原因發生日) → 過戶 → 再1-2月實價揭露
   （實證：環鴻購明基三豐精科廠，重訊114/08、過戶115/08/05）；交廠前3-6個月＝賣方安置需求高峰
3. **大宇範式**：處分≠經營不善，須交叉同期購置判讀（資產活化/產能重配置）；交易相對人一律追蹤
4. **注意力導向評分**：排序＝「今天該把注意力放哪」——已關注(active/有進度紀錄且無新事件) **-25**、
   新鮮未接觸(事件≤30天無紀錄) **+10**、新事件出現取消-25重新拉高。基礎分見排程 prompt
5. 公司md：重訊原文全文貼入（公開申報資料）；新聞只摘要＋連結（著作權）
6. **處分訊號的處理順序：先打電話、再查MOPS**（2026-08-10 和大實證）——
   `董事會決議處分` 的**決議日與委託決定通常是同一天**，真正的空窗不在決議後90天，而在**專任委託到期後**。
   和大查證花兩天且兩天皆失敗，電話花十分鐘就問出標的＋「已專任委託他家」（公告看不出來的通路狀態）。
   **查證與接觸應是同一個動作**（同理：頌勝一通電話同時解決查證與供件）。
   b類前兆（處分決議標的未明）的窗口意義一律以此重讀：**要問的不只是「賣什麼」，是「委託出去了沒」**。

## 三、基礎設施

| 元件 | 位置/ID |
|---|---|
| 紀錄試算表 | https://docs.google.com/spreadsheets/d/1W_7ab7fG9RuF9TYd-SIt710TxVt34aTAUfzLltmBkmg/edit（分頁：工作表1事件/觀察名單/經營進度） |
| Apps Script 端點(6版) | https://script.google.com/macros/s/AKfycbzmlpV1fpt1RWxVwZj8teUHWw4fs4zpix_3JqCVGX4SeMPIp5Di6_6m_YDRZn4fBQ4/exec ｜token=muju-trace-2026｜POST:事件/watch/progress/progress_del；GET:?list=watch\|records\|progress｜curl 要 -L 不要 -X POST；Windows 中文用 --data-binary @utf8檔；瀏覽器 POST 用 text/plain 避 CORS |
| GitHub | mac2good909777-commits/demand-trace（本機 C:\Claude\projects\demand-trace） |
| 公開版儀表板 | https://mac2good909777-commits.github.io/demand-trace/（睦聚淺色風；前端直接 fetch data；無登入）⚠️**全世界可讀**，資料檔內容等同公開 |
| **登入版儀表板** | https://script.google.com/macros/s/AKfycbwW2uvay_YEVMi62IuWebnKOKwI7uAMWIZ0Gzi-imiq2lsWAvht9jgxlvdBtQX5plY/exec ｜Google 登入＋白名單（ALLOW=mac2good909777〔owner〕, yubob0513〔助理〕, nibreeze〔同助理權限，2026-08-13 加〕）｜進度自動記「記錄者」｜登入部署 **v16**（2026-08-13 晚，沙箱代理 epGet/epPost/getMd）（部署ID AKfycbwW2uvay，同專案第2個部署，存取權=任何Google帳戶）｜⚠️首次開啟需 ~15-20s（伺服端抓4份資料）先空白再整頁跳出＝正常 |
| 分頁（兩版共用） | 💼重點經營(landing) → 總覽(權重排序) → 公司軌跡 → 事件時間軸(新→舊) → 🔭長期追蹤 → 封存資料 |
| 資料檔 | docs/data/records.json（事件）、watchlist.json（監控名單，欄位見下）、rp-baseline.json（實價基準313列）、docs/companies/*.md（軌跡檔）、docs/reports/（週報/回溯報告） |

**watchlist.json 欄位**（**每日排程務必維護、勿刪**）：
`company/ticker/tier/score/prob/kind/active/locs/reason/advice/added/md`
＋ `next`（下次關鍵日 YYYY-MM-DD）、`nextWhat`（那天要做什麼）、
`nextKind`（`窗口`＝事件驅動／`機制`＝週期供件無到期日／`追蹤`＝低頻回訪建議節奏）、
`plan`（時程表陣列 `[{d,w}]`，長期追蹤用）、`letters`（開發信三版 `{1,2,3}` 各 `{body,note}`）。
⚠️ **2026-08-13 起 `track`/`trackWhy` 已清空停用**——分類改由「經營進度」表的標記字驅動（見第十節），
排程**不要**再寫 track/trackWhy（長期追蹤只由人工判斷加入）。
儀表板據此渲染：總覽「⏰ 時間提醒」（7天內＋逾期）、各卡片時間提醒列、長期追蹤分頁（依 next 由近至遠排序）。
每份公司 md 亦有 `## ⏰ 經營時間提醒` 區塊，內容須與 watchlist 的 next/nextWhat 一致。

## 四、排程×3（claude.ai/code/routines）

| ID | 排程 | 時間 | 模型 |
|---|---|---|---|
| trig_01TfoA3oZujvQSveGMLFC14g | 每日監控＋協作教練 | 每天 03:00 | Opus 5 |
| trig_01FMPuuumP3KBqb75bexyqXs | 週一晨報（產業聚類/前兆掃描/推薦3家→docs/reports/） | 週一 07:03 | Opus 5 |
| trig_01MLey81YBUvPGjMz1ZbSNsb | 盤後快訊（當日重訊命中即寫） | 平日 17:00 | Sonnet 5 |

每日排程含：30家逐家重訊(14天)+新聞(7天)、全市場掃描(主旨無地點抓內文≤20則)、
深研輪替2家/日、原文回補3家/日、實價月檢(每月10日只比對新增列、聚焦交易日近一年)。

## 五、技術要點

- TWSE 當日重訊 JSON：https://openapi.twse.com.tw/v1/opendata/t187ap04_L（curl 可用）
- **ezsearch_query**（重訊歷史+原文，curl 可用）：POST https://mopsov.twse.com.tw/mops/web/ezsearch_query
  參數 step=00&RADIO_CM=2&TYPEK=all&CO_ID=代號(可留空全市場)&SUBJECT=關鍵字&SDATE/EDATE=西元YYYYMMDD
  回 JSON（開頭有BOM），HYPERLINK 欄＝該則公告全文連結；每次上限1000筆。MOPS ajax_* 直接 curl 會被擋
- 第51款授權類公告主旨不寫地點（藏內文）——曾漏判大統益，鐵則：必抓原文
- 雲端排程環境封鎖 script.google.com：讀取走 Drive MCP、寫入失敗寫 repo 待同步（儀表板讀 repo 不受影響）
- 上市櫃登記地址：TWSE t187ap03_L / TPEx mopsfin_t187ap03_O（英文）

## 六、名單現況（30家，2026-08-09）

**注意力榜首**：創新服務 7828（窗口剩29天・9/8到期・零接觸）；次為鴻勁 80（進行式買方・零接觸）
**長期追蹤**：和大工業 85→45（8/10電訪解鎖：標的＝大里仁化路廠房，但8/6董事會已**專任委託他家**、
告知不用再聯繫。不放掉——回訪日 2026-11-06／2027-02-06 專任期滿；被動監控：搜591/樂屋「大里 仁化路 廠房」
找出受託公司即可帶買方共同銷售；清查大里是否還有第二件未委託資產）
**進行中案件**：大統益×興國橡膠＝廠地議約中近簽約（**雙邊代理**，收斡促簽）
**配對線**：環鴻要買銅鑼（合作同業經營中）⟷ 艾姆勒銅鑼處分17個月未成交＋億光銅鑼標售
**開著的窗口**：雷虎大埔美購置9個月未完成、創新服務窗口剩29天零接觸、鴻勁進行式買方、
頌勝授權未用完、信邦台中分公司6.65億未落地（我方有進欣昱案窗口）
**服務線**：進欣昱台中產業園區土地興建出租（地號待確認）；大宇彰濱去化＋大雅招租
**經營中11家**：啟新/聯純/大統益/十全特好/信邦/興國/進欣昱/台灣暹勁/大宇/統一/大城長城

## 七、操作方式（Mac 日常）

- 看儀表板（每天03:00後更新）；經營動作在卡片上點選即存（開發接觸/購置經營/出售經營分組；✕可刪；「追蹤結案」→自動進封存頁）
- 丟情報給 Claude（新聞連結/謄本查證/聽到的風聲）→ 消化入系統
- 加公司：說「加入XX並研究」；升降級/封存：直接說
- 手動觸發排程：RemoteTrigger run + trigger_id

## 十、分類系統與登入版架構（2026-08-13 新增，重要）

### 10.1 卡片分類（3 類）
每張公司卡片與 md 檢視都有「歸類」切換列（`catSwitch()`）：
- 💼 **重點經營**（focus）｜🔭 **長期追蹤**（track）｜🗂 **封存**（archive）
- 封存＝從「公司軌跡」清單移除。

> **🤝 交易洽談已於 2026-08-13 晚整個移除**（Mac 決定）。分頁 tab6／view6、卡片上的
> 「設為交易洽談」按鈕、`renderDeal()`、`CAT_MARKS` 的 deal 對照、各處 `cat!=='deal'`
> 過濾條件、以及 `OWNER`／`isOwner()` owner-only 機制全部拿掉。
> 進度表裡舊有的「設為交易洽談」列不再被 `CAT_MARKS` 認得——`catOf()` 會自動往前找
> 上一個標記，都沒有就回退 watchlist `active→focus`（進欣昱因此自動回到重點經營）。
> 要復原就看 git 紀錄（移除前最後一版：commit `85e542a`）。
- **分類來源**：由「經營進度」表的**標記字**決定，非寫在 watchlist。對照表
  `CAT_MARKS={'設為重點經營':focus,'設為長期追蹤':track,'追蹤結案':archive}`。
  `catOf(公司)`＝取該公司在進度表最新的一筆標記字；若無標記則回退 watchlist `active→focus`。
- **長期追蹤初始為空**：只由 Mac 或助理人工判斷加入（點「設為長期追蹤」）；**排程不得自動貼**。
- **`track` 只能人工標記，排程一律不得寫入**。
- 事件時間軸：**新→舊**排序（新的在最上，`recordDate` 反向 localeCompare）。

### 10.2 使用者分權（交易洽談的 owner-only 已移除；2026-08-14 改為助理視圖）

**現行：助理視圖**（Mac 2026-08-14 決定）
- `ASSISTANTS=['yubob0513@gmail.com','nibreeze@gmail.com']`（兩人同一組設定）
- `isAssistant()` 為真時，隱藏 `HIDE_FOR_ASSISTANT=[6,7]`＝**🏢 公司集團**與**🔗 產業鏈**兩頁：
  ①`renderBoot` 把 tab6/tab7 設為 `display:none` ②`show()` 開頭擋掉這兩個索引
  ③集團標籤的 `+N` 對助理不做成連結（否則會連到看不到的頁）。
- 助理看得到：重點經營／總覽／公司軌跡／事件時間軸／長期追蹤／封存——
  理由：助理負責**前期經營、客戶電訪、開發函**，結構資料頁用不到，藏起來讓畫面專注。
- ⚠️ **這是前端隱藏，不是權限**。DevTools 可繞過；且公開版 github.io 無登入、全部可見。
- ⚠️ 比對必須**明確列舉 email**。公開版沒有 `DT_USER` → 判為非助理 → 看到全部（刻意如此）。
  **切勿寫成 `!DT_USER||...`** —— 交易洽談時代就是這樣寫錯，讓公開版被當成有權限。

**歷史**：交易洽談的 owner-only 機制（`OWNER`／`isOwner()`）已於 08-13 隨功能移除。
`DT_USER` 仍保留並由 doGet 注入完整 email（`mac2good909777@gmail.com`），
但現在只用於**進度紀錄的「記錄者」欄位**，不再控制任何顯示。
（歷史備忘：當時 isOwner 用嚴格比對，就是為了讓沒有 DT_USER 的公開版一定判為 false。
若日後要重做分權，記得這條——別寫成 `!DT_USER||...`，那會讓公開版被當 owner。）

### 10.3 登入版空白 bug 與修法（务必理解，否則會再踩雷）
- **症狀**：登入版整頁空白或永遠卡「載入中…」，但 github.io 公開版正常。
- **根因**：登入版 HTML 跑在 HtmlService 沙箱(googleusercontent) 內。沙箱中前端對 `script.google.com`（EP 的 `?list=progress`/`?list=needs`，即 loadProg/loadNeeds）的 fetch **會卡住而不 reject**→`Promise.all` 永不 resolve→`.then` 內的渲染與 tab6 揭示碼都不執行。**try/catch 對「hang」無效**（只擋 reject）。
- **修法＝伺服端注入 `window.__DATA`**：
  - Apps Script `doGet` 登入分支：UrlFetchApp 伺服端抓 records.json+watchlist.json（github.io）＋ EP 的 progress+needs，組成
    `window.__DATA={recs,watch,prog,needs}` 內嵌進 `<head>`（`</` 用 `String.fromCharCode(92)` 轉義防斷 script）。
  - index.html `boot()`：若見 `window.__DATA` 就直接用、**沙箱內不做任何跨域 fetch**；否則（公開版）照常 fetch。所有 fetch 加 `.catch` 保底。
- **代價**：登入版 doGet 做 4 次 UrlFetch，首屏 ~15-20s，會**先空白、再整頁一次跳出**——這是正常，不是壞掉。可優化（快取/減少 fetch）列待辦。

#### 10.3b 寫入路徑同樣中招（2026-08-13 下午修，**上次只修了讀取**）
- **上次的修法只處理 `boot()` 的讀取**，所有**寫入**仍是 client `fetch(EP)`：
  `setCat`／`saveProg`／`delProg`／`loadProg`／`saveNeed`／`loadNeeds`。
  因此登入版**點分類、記進度、刪進度、送需求單全部靜默卡死**——
  POST 其實有送出去（資料有寫進試算表），但接在後面的 `await loadProg()`（GET EP）hang 住，
  `renderAll()` 永遠不執行 → 畫面完全沒反應。
- **實證**：進度表出現兩筆同一分鐘的「進欣昱企業／設為交易洽談」＝點了沒反應又點一次。
- **修法**：index.html 新增 `epGet()／epPost()` 抽象層——
  `SANDBOX=!!(window.google&&google.script&&google.script.run)` 為真時走
  `google.script.run`（HtmlService 正規通道，不受跨域限制），由伺服端代打 EP；
  公開版 github.io 走原本的 `fetch`，行為不變。
- `openMd()` 也一併修：沙箱內相對路徑 `companies/*.md` 會指到 googleusercontent 抓不到，
  改走伺服端 `getMd()`，並補 `.catch` 保底（原本失敗會整個卡住不顯示）。
- **.gs 端新增三個函式**：`epGet(kind)` / `epPost(body)` / `getMd(file)`，
  全部用 UrlFetchApp 代打。原始碼備份於 repo `tools/appscript-sandbox-proxy.gs`。
- ⚠️ **鐵則：登入版沙箱內不得出現任何 client 端對 script.google.com 或相對路徑的 fetch。**
  日後新增任何會打 EP 的功能，一律走 `epGet`/`epPost`，不要再直接寫 `fetch(EP,...)`。

### 10.4 改版與部署流程（重要差異）
- 改 index.html / 資料 → `git push`（公開版即時生效；登入版下次開啟時伺服端重抓，也會拿到新的）。
- 改 Apps Script（doGet/doPost 邏輯）→ 在 script.google.com 編輯器改、**存檔用工具列磁碟鈕**（Ctrl+S 有時不觸發）、
  然後「部署→管理部署作業→選登入部署(AKfycbwW2uvay)→鉛筆→版本下拉→建立新版本→部署」。目前登入部署為 **v16**（2026-08-13 晚，沙箱代理）。
- **.gs 專案位置**：容器綁定在紀錄試算表上，名稱「**需求軌跡寫入端點**」，只有一個檔案 `程式碼.gs`。
  Drive 檔案清單搜不到（bound script 不獨立列出）——從 script.google.com/home 或試算表「擴充功能→Apps Script」進入。
  既有內容：`TOKEN`／`SHEET_NAME`／`ALLOW` 三個常數＋`doPost`／`doGet`／`__pj`＋（新增）`epGet`／`epPost`／`getMd`。
- ⚠️ 雲端排程環境封鎖 script.google.com，改 .gs 必須在有瀏覽器的本機 session 手動做。

## 八、待辦與已知問題

- [ ] 兩份自建表開「知道連結者可檢視」（實價登錄表 1z6s3IPZ...、監控表 1Gkce8l...）——雲端可走 Drive MCP 已可讀，此項改為選配
- [ ] 試算表寫入通道（雲端封鎖）反向同步方案：Apps Script 定時從 GitHub 拉 records.json 回寫（未做）
- [ ] 進欣昱興建出租案：待地號/坪數/時程 → 租賃行情+預租招商提案
- [ ] 頌勝授權日期查證；高鋒/錦明窗口逾期處置（見週報）
- [ ] **每日排程 prompt 要補一段**（一併補上「不得寫 track／deal 標記」的禁令）：維護 watchlist 的 `next/nextWhat/nextKind/track/trackWhy/plan`
      ——事件更新時同步推進下次關鍵日；`next` 已逾期而無動作者，在晨報點名。否則新欄位會被排程覆寫掉
- [ ] 和大開發信已產出（20260812-和大工業-開發信.pptx，改自 DEVELOPMENT LETTER 模板）；
      該模板右側 QR 為「台中工業區行情」，和大在大里工業區外，若有大里版行情頁建議換 QR
- 快訊/晨報執行中若 sheet POST 失敗屬正常（封鎖），資料以 repo 為準

### 2026-08-13 新增待辦
- [ ] **公開版移除範圍待 Mac 決定**（登入版目前伺服端從 github.io 抓殼+資料，不能直接關 Pages）：
      (A)導引頁替換〔推薦，小工作量：index.html→導引頁，真儀表板改名 app.html 給登入版抓〕
      (B)完全自包含〔HTML 內嵌 .gs、資料改讀 Sheet/Drive，可整關 Pages，但每次改版要重部署 .gs〕
      (C)暫不移除。**尚未動手，等指示。**
- [ ] 登入版首屏 ~18s 可優化（快取／減少伺服端 4 次 UrlFetch）
- [ ] **登入版寫入通道待實點驗證**（v16 已部署、首屏已確認正常，
      但「點分類／記進度會不會即時反映」還沒實際點過一次）
- [ ] 進度表仍留有兩筆「進欣昱企業／設為交易洽談」（12:29）＋其他經營紀錄列，
      功能移除後不影響分類判斷，但會以 chip 顯示在卡片上；要清就在畫面點 ✕
      （`progress_del` 由後往前找到第一筆就 return，一次只刪一列，安全）
- [ ] 白名單 nibreeze@gmail.com 已加入 ALLOW（v15 部署）；注意 nibreeze 是另一 Google 帳號（CLAUDE.md 標「勿用」指的是別拿它當專案根/Drive，非指登入白名單）

## 九、相關文件

- 本地專案：C:\Users\dell\Documents\Claude-DT\projects\20260808-需求軌跡\（系統設計.md、觀察名單建議、appscript-doPost.gs）
- 本機記憶：demand-trace-system.md（session 自動載入）
- Drive 鏡像：ClaudeSetting/記憶-需求軌跡監控系統-20260808
- 週報第1期＋2025回溯掃描：repo docs/reports/

## 十一、快速接手指引（新 session 讀這段就懂現況）

1. **系統現況**：公開版(github.io)＋登入版(Apps Script v14/v15) 並存運轉；三個排程照跑。
2. **登入版是主力**（有登入、有記錄者）；公開版無登入，且**全世界可讀**。
3. **最近一次工作(2026-08-13)**：卡片分類系統→**移除交易洽談**（含 owner-only 機制）、
   修好登入版空白(伺服端注入)與**所有寫入互動卡死**(沙箱走 google.script.run)、加白名單 nibreeze。
4. **唯一未完成**：公開版移除（待 Mac 選範圍 A/B/C，見待辦）。
5. **改 .gs 一定要**：工具列磁碟鈕存檔 → 管理部署作業 → 登入部署(AKfycbwW2uvay) → 建立新版本 → 部署。
6. 別做的事：排程勿自動寫 watchlist 的 track/trackWhy；isOwner 勿改回寬鬆版；未確認登入版正常前勿關 GitHub Pages。

## 十三、開發信＝提示詞產生器（2026-08-14 改，Mac 定調）

- **舊做法**：`watchlist.json` 的 `letters` 欄預存三版信件全文。問題有三——
  ①三版只是套版換公司名與地名，沒有新聞、行情、針對性
  ②依新聞客製的內容會過時（第2/3版是 3、6 個月後才寄）
  ③`watchlist.json` 公開可讀，等於把話術與署名放上網。
- **新做法**：「📄 開發信」按鈕改為產生 **貼給 Claude 的提示詞**（`letterPrompt(co,n)`），
  不預存任何信件內容。按下即自動帶入該公司的：
  區域 `locs`、集團 `group`、**最近 3 筆事件**（RECS）、**上次接觸紀錄與累計筆數**（PROG，
  已排除 CAT_MARKS 分類標記字），並依接觸筆數自動預選輪次（0 筆→第1次／1-2→第2次／3+→第3次）。
- 提示詞內含撰寫要求：300 字以內、依輪次調整語氣、**不得臆測對方買賣意圖**、
  不得提及我方與其他公司的往來、行情資料放信末、固定開頭與結尾格式。
- **助理流程**：公司軌跡→點公司→📄 開發信→（有行情資料就貼進【區域行情資訊】欄）→
  複製提示詞→貼到 Claude→產出信件。
- ⚠️ `letters` 欄位**已停用但資料還在** `watchlist.json` 裡（仍公開可讀），待決定是否清除。

## 十二、公司集團＋產業鏈（2026-08-14 新增）

- **交易洽談分類已於 08-13 移除**（Mac 指示，另一 session 執行）；同 session 加了 SANDBOX/gsRun 修登入版寫入卡死。現行分頁：重點經營/總覽/公司軌跡/事件時間軸/長期追蹤/**公司集團**/**產業鏈**/封存。
- **公司集團**：watchlist.json `group` 欄 `[{"n":"公司名","r":"關係"}]`。公司軌跡卡片顯示 ≤3 個 🏢 標籤（超過 +N 點入集團頁）；集團頁列完整關係，名單內公司帶 ↗ 可點開軌跡檔。已填 11 家；其餘由每日排程深研輪替補齊。
- **產業鏈**：資料**內嵌** index.html 的 `/*CHAIN-DATA-START*/…/*CHAIN-DATA-END*/` 區塊（刻意不做獨立 json——登入版沙箱 fetch 會出問題）。7 條鏈，rows=上/中/下游，nodes `{n,w:1=名單內可點}`，note=🎯配對線。
- **epGet 8 秒逾時**（AbortController）：EP hang 不 reject 的保險，公開版不再卡「載入中」。
### 12.1 維護節奏＝一次性建置＋半年更新（Mac 2026-08-14 定調，**已改**）
這兩份是**結構資料**（股東組成、上下游關係），不是每日變動的動態資料，
因此 **不由每日排程維護**——第一次建置完成後，**約半年更新一次**即可。

- **最後更新：2026-08-14｜下次建議：2027-02-14**
- 日期寫在 index.html 的 `GROUP_UPDATED` / `CHAIN_UPDATED` 兩個常數，
  頁面標題會自動顯示「🗓 最後更新 X・下次建議 Y（半年一次）」，
  下次日期由 `plusHalfYear()` 推算，**改資料時只要改常數那一個日期**。
- ⚠️ **每日排程 prompt 要拿掉「深研輪替一併查股東組成寫 group」那段**
  （原設定會讓排程天天動 group，與一次性建置的定位衝突）。
  產業鏈的 CHAIN 區塊同理，排程不再增補。
- 半年到期要更新時：直接說「更新公司集團／產業鏈」，一次補齊後把常數日期改掉。

---
交接檔產出：2026-08-09｜最後更新：2026-08-13｜新 session 開場說「需求軌跡」＋讀本檔即可完整接手
