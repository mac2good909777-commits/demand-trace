# 待同步至 Google 試算表的事件列

**目前狀態：1 筆待同步（2026-08-16 雲端排程產生，寫入端遭封鎖）**

`records.json` 26 筆 ＞ 試算表「事件紀錄」分頁 25 筆，差 1 筆。

## 待同步清單

| 公司 | 事件日期 | 事件類型 | 摘要 |
|---|---|---|---|
| 大立光(3008) | 115/07/27 | 上下游動態 | 董事會決議與大陽科技股份轉換（合併），大陽科技（西屯工業區七路18號）為消滅公司，換股基準日 2026/12/04 |

補寫方式（在可直連 BASE 的本機 DT 執行）：

```bash
python tools/backfill_sheet.py            # 乾跑，只列出缺哪些
python tools/backfill_sheet.py --write    # 確認後實際寫入
```

判重鍵＝(公司, 事件日期, 事件類型)，與每日排程一致。

## ⚠️ 2026-08-16 發現：雲端排程仍在跑，且與本機 DT 重疊

2026-08-13 的處置記載為「排程全數搬回本機 DT，雲端三支 routine 停用保留」，
但**雲端 routine 實際仍在每日約 UTC 19:00（台北 03:00）觸發**：
`daily/2026-08-15` 分支與 PR #1 即為雲端 routine 於 2026-08-14 19:17 UTC 產生，本次（2026-08-16）
再產生 `daily/2026-08-16`。

雲端執行環境的 egress proxy 封鎖範圍與先前一致，本次實測：

| 端點 | 結果 |
|---|---|
| `script.google.com`（BASE 讀寫） | CONNECT 403 — 讀寫皆不可用 |
| `openapi.twse.com.tw`（上市重訊 API） | egress blocked |
| `mopsov.twse.com.tw` / `www.twse.com.tw`（重訊原文、ezsearch） | egress blocked |
| 一般網站直連 curl | 全數 000（僅 Anthropic 網域與套件源放行） |
| Google Drive（MCP） | ✅ 可用——`list=watch/progress/needs` 已改由直接讀試算表 `1W_7ab…` 取得 |
| WebSearch 工具 | ✅ 可用——新聞面查證仍可進行 |

**影響**：雲端跑時「當日重訊」與「重訊原文」兩步驟結構性失效，只能靠新聞面補；
且事件無法寫回試算表，只能積在 repo。

**建議二擇一，請現傑決定：**

1. **關掉雲端 routine**，全部交本機 DT——需求軌跡的重訊步驟才會完整；或
2. **保留雲端 routine 當備援**，但明確定位為「新聞面＋深研＋經營協作」，
   重訊與試算表寫入一律由本機 DT 補，並固定用 `backfill_sheet.py` 收尾。

在做出決定前，兩邊同時跑會持續產生「repo 有、試算表沒有」的落差。

## 歷史：2026-08-08 ～ 08-12 的積壓與解除

需求軌跡原本跑在 Claude 雲端 routine，該執行環境的 egress proxy 擋掉
`script.google.com`（CONNECT 403 / curl exit 56），POST 寫入連續多日失敗，
事件只進了 repo 的 `records.json`，試算表停在 `2026-08-09 環鴻科技`（8 列），
累積 14 筆未同步。同一批封鎖也擋掉 `openapi.twse.com.tw`、`www.tpex.org.tw`、
`mopsov.twse.com.tw` 與多數新聞站，導致重訊步驟連續三日靜默失效。

**2026-08-13 處置**：排程全數搬回本機 DT（無網路限制，已實測 BASE 可直連），
雲端三支 routine 停用保留（**實測未生效，見上節**）。14 筆以 `tools/backfill_sheet.py` 補寫完成，全部回 `{"ok":true}`。

## 日後怎麼補

若再出現寫入失敗累積，直接跑：

```bash
python tools/backfill_sheet.py            # 乾跑，只列出缺哪些
python tools/backfill_sheet.py --write    # 確認後實際寫入
```

腳本已處理兩個坑：

1. **日期格式**：試算表回 ISO UTC 時間戳，民國年被當成西元年序列化，
   古早年份套用台北 LMT（+8:06）而非 +8:00 —— 故用「加 12 小時取最近一天」正規化。
2. **只到月的日期**（如 `2026/01`）寫入後會被補成完整日期，反向比對不到 —— 改用年-月前綴比對，
   避免重複寫入。
