# 待同步至 Google 試算表的事件列

**目前狀態（2026-08-27）：雲端環境端點全封鎖，本輪 1 筆回補事件列待同步；`watch` 分頁寫入功能仍故障。**

---

## 🔴 2026-08-27 03:00（雲端輪）：端點封鎖依舊，1 筆回補待同步

同日 **02:55 本機 DT 輪已完成當日重訊輪並 push**（8/26 六則：科沃斯先進、穗曄實業、萬潤科技、矽品、友達等），
本輪於其後 8 分鐘觸發，**當日 02:55～03:03 之間不可能有新重訊**，故不重複掃描，
改做不依賴封鎖端點的工作（深研／group／CHAIN／回補／advice）。

| 端點 | 結果 |
|---|---|
| `script.google.com`（BASE，讀寫皆是） | CONNECT 403（curl exit 56），3 次重試全失敗 |
| `openapi.twse.com.tw` | CONNECT 403／WebFetch 回 `EGRESS_BLOCKED` |
| `www.tpex.org.tw` | CONNECT 403 |
| `www.cnyes.com`、`www.chiuho-crs.com` 等一般網站 | WebFetch 一律 `EGRESS_BLOCKED`（本輪 WebFetch 全數不可用） |
| Google Drive 連接器（讀試算表 1W_7ab…） | ✅ 三分頁（觀察名單／經營進度／需求描述）皆讀到 |
| WebSearch | ✅ 本輪唯一可用的對外查證管道 |

**待同步事件列 1 筆**（POST 不可用，先記於此）：

| 公司 | 代號 | 事件日期 | 類型 | 摘要 | 優先級 |
|---|---|---|---|---|---|
| 萬潤科技 | 6187 | 114/11/28 | 取得資產（回補） | 董事會決議「擬購置苗栗竹南廠房」＋資金貸與美國子公司 All Ring Tech USA LLC——即現持有之竹南群義路69號來源。金額未見於新聞，MOPS 原文待端點恢復回補 | A-立即接觸 |

來源：<https://money.udn.com/money/story/5612/9170188>

---

## ✅ 2026-08-21（本機 DT 02:30 輪）：事件列 3 筆全部 POST 成功

本輪端點全部可用，**無網路封鎖**（與 2026-08-20 第二輪雲端環境的 403 情況不同）：

| 端點 | 結果 |
|---|---|
| `script.google.com`（BASE，GET `?list=watch/progress/needs/records`） | ✅ |
| `script.google.com`（BASE，POST 事件列） | ✅ 3 筆全部 `{"ok":true}` |
| `mopsov.twse.com.tw/mops/web/ezsearch_query` | ✅ 15 組關鍵字查詢全通，277 筆去重結果 |
| `mopsov.twse.com.tw/mops/web/ajax_t05sr01_1`（重訊原文） | ✅ 7 則原文全取得 |
| WebSearch | ✅ |

**已寫入事件紀錄分頁的 3 筆**：鴻勁精密（8/20 日月光採購 10.95 億）、
矽品精密工業（8/11 斗六千億動土）、南亞科技（8/20 雲林選址，新聞先行）。

### 🔴 `action=watch` 仍然無效（第 5 次確認）

本輪 POST `{action:"watch", company:"南亞科技", ticker:"2408", reason:"..."}`
→ 回 `{"ok":true,"added":0}`，`?list=watch` 讀回仍為 **31 列（含標題）＝30 家**，
`watchlist.json` 已為 **40 家**。與 2026-08-19 實測結論一致，故障未修復。

---

## ℹ️ 2026-08-19 03:00（雲端第二輪）：無待同步事件列

同日 02:55 本機 DT 已完成當日重訊輪（矽品 8/18 設備 10.29 億、頌勝、聯詠三筆皆已 POST 成功）。
03:00 另有一支**雲端** routine 觸發，該環境的 egress proxy 封鎖情況與 2026-08-08～08-12 相同：

| 端點 | 結果 |
|---|---|
| `script.google.com`（BASE） | CONNECT 403（curl exit 56），讀寫皆不可用，9 次重試全失敗 |
| `openapi.twse.com.tw` | CONNECT 403 / WebFetch 回 `EGRESS_BLOCKED` |
| `mopsov.twse.com.tw`、`www.twse.com.tw`、`www.tpex.org.tw` | CONNECT 403 |
| Google Drive 連接器、WebSearch | ✅ 可用（本輪即靠這兩條做事） |

因此該輪**未執行**重訊掃描與實價登錄步驟（避免與本機輪重複，且端點不可達），
改做不依賴封鎖端點的工作：深度重研 2 家（大立光、聯純科技，含集團股東組成）、
需求匹配（統一／信邦）、回訪到期與停滯提醒（興國橡膠）、產業鏈 CHAIN 增補。

**沒有新事件列產生，故本檔無新增待同步項目**——watchlist.json／公司 md／index.html 的
CHAIN 區塊改動已直接進 repo。若本機輪日後要接手，直接 `git pull` 即可，無需回補試算表。

---

## 🔴 未解問題 1：`action=watch` 寫入無效（回 ok 但不落地）

2026-08-19 排程實測，四種 payload 形狀全部無法真正寫入 `觀察名單` 分頁：

| payload 形狀 | 回應 | 實際是否落地 |
|---|---|---|
| `{action:"watch", company, ticker, reason, ...}` | `{"ok":true,"added":0}` | ❌ |
| `{action:"watch", watch:[{company,ticker,reason}]}` | `{"ok":true,"added":0}` | ❌ |
| `{action:"watch", rows:[[公司,代號,原因]]}` | `{"ok":true,"added":1}` | ❌（**回報 added:1 但用 Drive 讀原始試算表確認未寫入**）|
| `{action:"addWatch", company, ...}` | `{"ok":true}` | ❌（見問題 2）|

`rows` 形狀回 `added:1` 最容易誤判成功——2026-08-18 排程「補建 7 家漏登 watch 分頁」
就是被這個假成功騙過去，實際一列都沒進。**驗證一律要用 Drive 連接器直接讀試算表
`1W_7ab7fG9RuF9TYd-SIt710TxVt34aTAUfzLltmBkmg` 的原始內容，不要只信 POST 回應，
也不要只信 `?list=watch`（該 GET 端點與寫入端點讀寫的範圍可能不一致）。**

### watch 分頁目前缺的 9 家

試算表 `觀察名單` 分頁停在 2026-08-09、共 30 家；`watchlist.json` 為 39 家。缺：

| 公司 | 代號 | 分數/級別 |
|---|---|---|
| 大立光 | 3008 | A 88 |
| 矽品精密工業 | 3711 | A 85 |
| 台化 | 1326 | A 80 |
| 艾姆勒 | 2241 | A 55（在分頁內，資料已舊）|
| 振鋒企業 | 4562 | B 45 |
| 全新光電 | 2455 | C 25 |
| 巧新科技 | 1563 | C 35 |
| 永泉豐企業 | 未上市 | C 30 |
| 亞泰冷藏 | 未上市 | C 25 |
| 聯詠 | 3034 | C 30（2026-08-19 新增）|
| 南亞科技 | 2408 | B 45（2026-08-21 新增，第 10 家）|

**修法**：需要人工檢視 Apps Script 的 `doPost` 是否有處理 `action==='watch'` 分支、
以及它 append 的目標分頁名稱與 `?list=watch` 讀取的是否為同一個。修好前，
watch 分頁請以 `watchlist.json` 為準，儀表板若讀 watch 分頁會少 9 家。

## 🔴 未解問題 2：事件紀錄分頁有 1 列待人工刪除

上述第四種 payload（`action:"addWatch"`）未被辨識，落到預設的事件寫入分支，
在「事件紀錄」分頁產生一列只有前三欄、其餘全空的殘列：

```
2026-08-19 2:48 | 聯詠 | 3034 | （其餘 9 欄全空）
```

這是本次排程測試 payload 形狀時造成的，**請人工刪除該列**。
聯詠的正式事件列已於同一分頁 `2026-08-19 2:46` 正確寫入（擴產設廠／銅鑼研發運算中心），
刪掉 2:48 那列不會遺失資料。

**教訓**：不要對正式端點試打未知的 `action` 值——無法辨識的 action 會 fallthrough 到
事件寫入，污染資料。要試形狀請先確認 Apps Script 原始碼。

---

## 歷史：2026-08-08 ～ 08-12 的積壓與解除（已結案）

需求軌跡原本跑在 Claude 雲端 routine，該執行環境的 egress proxy 擋掉
`script.google.com`（CONNECT 403 / curl exit 56），POST 寫入連續多日失敗，
事件只進了 repo 的 `records.json`，試算表停在 `2026-08-09 環鴻科技`（8 列），
累積 14 筆未同步。同一批封鎖也擋掉 `openapi.twse.com.tw`、`www.tpex.org.tw`、
`mopsov.twse.com.tw` 與多數新聞站，導致重訊步驟連續三日靜默失效。

**2026-08-13 處置**：排程全數搬回本機 DT（無網路限制，已實測 BASE 可直連），
雲端三支 routine 停用保留。14 筆以 `tools/backfill_sheet.py` 補寫完成，全部回 `{"ok":true}`。

## 事件列日後怎麼補

若事件列再出現寫入失敗累積，直接跑：

```bash
python tools/backfill_sheet.py            # 乾跑，只列出缺哪些
python tools/backfill_sheet.py --write    # 確認後實際寫入
```

判重鍵＝(公司, 事件日期, 事件類型)，與每日排程一致。腳本已處理兩個坑：

1. **日期格式**：試算表回 ISO UTC 時間戳，民國年被當成西元年序列化，
   古早年份套用台北 LMT（+8:06）而非 +8:00 —— 故用「加 12 小時取最近一天」正規化。
2. **只到月的日期**（如 `2026/01`）寫入後會被補成完整日期，反向比對不到 —— 改用年-月前綴比對，
   避免重複寫入。

---

## ✅ 2026-08-20 02:xx（本機 DT）：2 筆事件列已成功寫入

| 公司 | 事件日期 | 類型 | POST 回應 | Sheet 驗證 |
|---|---|---|---|---|
| 頌勝科技 7768 | 2026-08-19 | 擴產設廠（中科建廠追加 3.4→7.5 億） | `{"ok":true}` | ✅ 已用 `?list=records` 讀回確認落地 |
| 頌勝科技 7768 | 2026-08-19 | 取得資產（十六路14號 510.12 坪補充簽約） | `{"ok":true}` | ✅ 同上 |

**無待同步積壓。** `script.google.com` 本機直連正常，事件寫入端點運作無誤。

### 🔴 未解問題 1（watch 分頁寫入無效）：狀態不變

仍缺 9 家，以 `watchlist.json` 為準。今日 **頌勝科技 30→80 分、C→A 級**，
該筆已在 watch 分頁內但**分數／級別為舊值**——這是 watch 分頁寫入故障的另一個表現面：
不只新增不進去，既有列也無法更新。儀表板若讀 watch 分頁，頌勝會顯示為 C 級 30 分（錯誤）。

### 🔴 未解問題 2（事件紀錄分頁殘列）：**仍待人工刪除**

```
2026-08-19 2:48 | 聯詠 | 3034 | （其餘 9 欄全空）
```

2026-08-20 用 `?list=records` 讀回時該列仍在（第 34 列位置）。請人工刪除。
聯詠正式事件列在 `2026-08-19 2:46`，刪掉 2:48 那列不會遺失資料。

---

## 2026-08-20（第二輪・03:0x）：🚨 端點與資料源全面遭網路政策封鎖

本輪執行時，**除 WebSearch 與 GitHub 外的所有外部網域皆被 egress proxy 以 403 CONNECT 拒絕**。
同日 02:46 的第一輪尚可正常存取 MOPS，兩輪相距僅約 17 分鐘，屬**環境政策變動、非目標站台故障**。

### 封鎖清單（curl 與 WebFetch 兩路皆失敗）

| 網域 | 用途 | 影響的每日步驟 |
|---|---|---|
| `script.google.com` | 試算表讀寫端點（BASE） | 步驟 1 讀取、輸出 A 全部 POST |
| `openapi.twse.com.tw` | 上市當日重訊清單 | **步驟 3 全市場掃描** |
| `mopsov.twse.com.tw` / `www.twse.com.tw` | 重訊原文 ezsearch_query | **步驟 4 原文取證** |
| `news.cnyes.com` / `chinatimes.com` / `money.udn.com` / `tw.stock.yahoo.com` / `house.ettoday.net` / `ctee.com.tw` | 新聞查證 | 步驟 5 原文查證（僅剩 WebSearch 摘要） |
| `goodinfo.tw` / `info.technews.tw` | 股權與公司登記 | 步驟 7 深研佐證 |

### 本輪處置

- **讀取**：依既定備援改由 **Drive 讀 `1W_7ab7fG9RuF9TYd-SIt710TxVt34aTAUfzLltmBkmg`**，成功取得 watch／progress／needs／records 四分頁全文。✅
- **寫入**：`script.google.com` 不可達 → 依鐵則**改寫 repo**，本輪所有產出僅落在 GitHub。
- **步驟 3／4（當日重訊掃描與原文取證）：無法執行。** 本輪**未宣稱**今日無新事件——是**查不到**，兩者不同，不可混為一談。
  同日第一輪（02:46）已完成 8/19 全市場掃描並落地 2 筆事件；8/20 當日重訊**尚未經任何查核**。
- **records.json 未新增任何列**（無經查證的新事件），符合「沒新事件不寫列」鐵則。
- 本輪 commit 內容限於**深研／集團組成／advice／CHAIN**（鐵則明列之例外）。

### ⏭ 待 MOPS 恢復後須回補

1. **8/20 當日全市場重訊掃描**（上市 t187ap04_L ＋ 上櫃 TPEx）與觀察名單逐一查核。
2. 鴻勁精密（7769）年報股權比例原文——本輪股權結構取自公開登記與新聞。
3. 信邦電子（3023）年報子公司持股比例原文。

### 🔴 未解問題 1（watch 分頁寫入無效）：狀態不變，且本輪無法驗證

仍缺 9 家，以 `watchlist.json` 為準。本輪因端點封鎖，**無法再次驗證寫入是否恢復**。

### 🔴 未解問題 2（事件紀錄分頁殘列）：**仍在，仍待人工刪除**

```
2026-08-19 2:48 | 聯詠 | 3034 | （其餘 9 欄全空）
```

本輪自 Drive 讀取的 records 分頁中該列**依然存在**（倒數第 6 列位置）。
聯詠正式事件列在 `2026-08-19 2:46`，刪掉 2:48 那列不會遺失資料。

---

## 2026-08-22（每日排程）

### ✅ 事件紀錄分頁：5 筆全部寫入成功

`{"ok":true}` ×5 —— 崇越(5434) 2026-08-17、台化(1326) 2025-12-12（回補）、
矽品(3711) 2026-08-21、鴻勁(7769) 2026-08-21、巨大(9921) 2026-08-21。
Drive 讀取複驗，五列皆在（記錄日期 2026-08-22 2:44）。

### 🔴 未解問題 1（watch 分頁寫入無效）：**本輪已驗證，確認仍壞**

本輪 MOPS／Apps Script 端點全部可直連，因此得以完整驗證：

| 送出的 payload | 回應 | 實際結果 |
|---|---|---|
| `{"action":"watch","company":"崇越","ticker":"5434","reason":"...","added":"..."}` | `{"ok":true,"added":0}` | **未寫入** |
| `{"action":"watch","watchlist":[{...}]}` | `{"ok":true,"added":0}` | **未寫入** |
| `{"action":"watch","items":[{...}]}` | `{"ok":true,"added":0}` | **未寫入** |
| `{"action":"watch","rows":[["崇越","5434","測試","2026-08-22"]]}` | `{"ok":true,"added":1}` | **仍未出現在 watch 分頁**（`?list=watch` 與 Drive 讀取皆為 31 列、無此列） |

重點：`rows` 變體回報 `added:1` 但**分頁裡找不到那一列**，也未誤寫進 records 分頁
（records 為 43 列＝原 38 ＋ 本輪 5，無「測試」字樣）。
也就是說 Apps Script 的 watch handler **回傳值與實際寫入不一致**，
不是 payload 欄位名的問題，是 handler 本身壞了。

**待人工處理**：開 Apps Script 編輯器檢查 `action=watch` 分支
（`added` 計數與 `appendRow` 的目標 sheet 是否對得上；參考先前 Result_D 欄位偏移的成因，
懷疑同樣是沒釘 `table_range` / 取錯 sheet 物件）。

**目前缺口：watch 分頁 31 列 vs `watchlist.json` 42 家，缺 11 家。**
本輪新增未同步者：**崇越(5434)、巨大(9921)**。
一律以 `docs/data/watchlist.json` 為準。

### 🔴 未解問題 2（事件紀錄分頁殘列）：**仍在，仍待人工刪除**

```
2026-08-19 2:48 | 聯詠 | 3034 | （其餘 9 欄全空）
```

本輪自 Drive 讀取的 records 分頁中該列**依然存在**。
聯詠正式事件列在 `2026-08-19 2:46`，刪掉 2:48 那列不會遺失資料。

### 📌 本輪另外發現：經營進度分頁的「記錄者」欄位是空的

`?list=progress` 回傳 52 列，每列 5 欄，但**第 5 欄（記錄者）全部為空字串**；
自 Drive 直讀該分頁亦同。因此本輪**無法依 SKILL 要求區分現傑（mac2good909777）
與助理（yubob0513）的動作**，回顧段落改以「動作類型」推定分工。
若要恢復記錄者辨識，需檢查表單寫入端是否有填該欄。
（對照：客需分頁的「記錄者」欄有值，統一與信邦兩列皆為 mac2good909777@gmail.com。）

---

## 2026-08-22 03:00（雲端輪）：無新事件，無待同步項目

本輪為**雲端**排程（cron `0 19 * * *` UTC ＝台灣 03:00），於本機輪 02:44 完成當日重訊輪、
02:59 推送 commit `aa28427` 之後 3 分鐘觸發。**當日重訊、實價登錄、深研（亞泰冷藏／全新光電）
皆已由本機輪完成，本輪不重複執行，也未產生任何新事件列。**

### 端點實測（與 2026-08-19 雲端輪相同的封鎖情況）

| 端點 | 結果 |
|---|---|
| `script.google.com`（BASE，讀＋寫） | ❌ CONNECT 403（curl exit 56）；proxy status 亦記錄兩筆 `connect_rejected` |
| `openapi.twse.com.tw/v1/opendata/t187ap04_L` | ❌ CONNECT 403 |
| `mopsov.twse.com.tw/mops/web/ezsearch_query` | ❌ CONNECT 403 |
| `www.tpex.org.tw` | ❌ CONNECT 403 |
| Google Drive 連接器（讀 1W_7ab...）、WebSearch | ✅ 本輪即靠這兩條做事 |

觀察名單／經營進度／需求描述／事件紀錄四個分頁，本輪改由 Drive 直讀 `1W_7ab7fG9RuF9TYd-SIt710TxVt34aTAUfzLltmBkmg` 取得，資料完整。

### 本輪產出（不依賴封鎖端點）

1. **深度重研 2 家**——補上本機輪新增但尚未建 `group`／`letters` 的兩家：
   - **崇越(5434)**：查出朴子廠與 2026/07 周邊土地取得的主體是**崇越石英製造廠股份有限公司
     （統編 12559233、電話 03-5981933、朴子廠：嘉義縣朴子市馬稠後園區十三路47號、嘉義分公司統編 29099142）**，
     崇越科技僅持股約 40%，是 2024 年與日本信越石英合資的獨立法人。
     **原建議的接觸電話 02-8797-8020（台北總部）會打錯主體**，已更正 advice 與 nextWhat。
     另建立先行指標：第四廠動工前，100% 子公司建越科技工程／孫公司台螢實業會先出現關係人工程承攬重訊。
   - **巨大(9921)**：補集團組成（捷安特投資 100% 收購載體、鼎鎂 60.39%→96.63%、
     交易相對人薩摩亞商眾御、創辦人劉金標 21,403,498 股／董事長劉湧昌 13,297,162 股、無上市控股母公司）。
2. **letters**：兩家的第 1／2／3 次版開發信全數補齊（崇越的收件主體寫崇越石英製造廠，不是 5434 本體）。
3. **CHAIN 產業鏈**：`docs/index.html` 新增兩條鏈——**半導體石英耗材鏈**（信越化學／信越石英→崇越石英製造廠→台積電／矽品／京元）
   與**自行車・輕金屬材料鏈**（鼎鎂→巨大／捷安特投資→Giant 通路）。JS 語法已用 `node --check` 驗證通過，共 9 條鏈。

### 未解問題（狀態不變，仍待人工）

1. `action=watch` 寫入無效——本輪端點被封鎖，無法再驗；缺口仍為 watch 分頁 31 列 vs `watchlist.json` **42 家**。
2. 事件紀錄分頁殘列 `2026-08-19 2:48 | 聯詠 | 3034 |（其餘 9 欄全空）` 仍在。
3. 經營進度分頁「記錄者」欄全空，仍無法區分現傑與助理的動作。


---

## 2026-08-23 02:35（本機輪）：5 筆新事件已寫入，3 家新監控公司仍寫不進 watch 分頁

### ✅ 事件紀錄分頁：5 筆全部 `{"ok":true}`

| 公司 | 事件日 | 類型 |
|---|---|---|
| 友達 2409 | 2026-07-28 | 處分資產（華亞廠 197 億售廣達） |
| 廣達 2382 | 2026-07-28 | 取得資產（197 億買友達華亞廠） |
| 光罩 2338 | 2026-06-10 | 處分資產（竹南廠 28 億售矽品） |
| 矽品 3711 | 2026-06-10 | 取得資產（買光罩竹南廠・回補） |
| 矽品 3711 | 2026-06-25 | 擴產設廠（中科二林＋南科租地・回補） |

### 🔴 未解問題 1（`action=watch` 寫入無效）：**仍在**

本輪三家新監控公司 POST `action=watch` 全部回 **`{"ok":true,"added":0}`**——
回傳 ok 但 `added:0`，代表 Apps Script 端收到請求卻沒有實際新增列。
**這是本機輪、端點可直連的情況下測得的結果，可排除網路封鎖因素，確定是寫入端邏輯問題。**

本輪未同步者：**友達(2409)、廣達(2382)、光罩(2338)**。
加上先前累積：**缺口擴大為 watch 分頁 31 列 vs `watchlist.json` 45 家，缺 14 家。**
一律以 `docs/data/watchlist.json` 為準。

### 🔴 未解問題 2（事件紀錄分頁殘列）：**仍待人工刪除**

```
2026-08-19 2:48 | 聯詠 | 3034 |（其餘 9 欄全空）
```
聯詠正式事件列在 `2026-08-19 2:46`，刪掉 2:48 那列不會遺失資料。

### 🔴 未解問題 3（經營進度分頁「記錄者」欄全空）：**仍在**

本輪 `?list=progress` 回傳 **52 列 × 5 欄**，第 5 欄（記錄者）**依然全部為空字串**。
因此仍**無法依 SKILL 要求區分現傑（mac2good909777）與助理（yubob0513）的動作**，
本輪回顧段落一樣以「動作類型」推定分工（電訪／寄信＝助理前端；委託／確認需求＝現傑）。
若要恢復記錄者辨識，需檢查表單寫入端是否有填該欄。

---

## 2026-08-24 02:5x（本機輪）：2 筆事件已寫入，`action=watch` **第 7 次確認仍壞**

### ✅ 事件紀錄分頁：2 筆全部 `{"ok":true}`

| 公司 | 事件日 | 類型 |
|---|---|---|
| 美光台灣 | 2026/01/17 | 取得資產（18 億美元／約 569 億台幣購力積電銅鑼 P5 廠） |
| 美光台灣 | 2026/03/16 | 擴產設廠（完成收購、啟動無塵室改裝、2026 年底二期動工） |

### 🔴 未解問題 1（`action=watch` 寫入無效）：**仍在，且缺口擴大到 15 家**

本輪為新監控公司**美光台灣**送出兩種 payload，皆回 `{"ok":true,"added":0}`，`?list=watch` 讀回仍為 31 列：

| payload | 回應 | 是否落地 |
|---|---|---|
| `{"action":"watch","company":"美光台灣","ticker":"...","reason":"...","added":"2026-08-24", ...}` | `{"ok":true,"added":0}` | ❌ |
| `{"action":"watch","watch":[{company,ticker,reason,added}]}` | `{"ok":true,"added":0}` | ❌ |

**本輪另做了完整落差比對**（`?list=watch` vs `watchlist.json`），結論比先前更明確：

- watch 分頁 **30 家**，加入日期只有 **2026-08-07 與 2026-08-08 兩天**——
  也就是說 **`action=watch` 自 2026-08-08 之後從未成功寫入過任何一列**，不是偶發。
- `watchlist.json` 現為 **46 家**，缺口 **16 家**（含本輪新增的美光台灣）。
- 缺的 15 家（不含美光台灣）：**矽品精密工業(A 95・全名單第一順位)**、**崇越(A 85)**、
  **大立光(A 88)**、**台化(A 82)**、南亞科技、友達、廣達、光罩、聯詠、巨大、
  巧新科技、振鋒企業、全新光電、永泉豐企業、亞泰冷藏。

**分數最高的前幾家幾乎全都不在 watch 分頁裡**——若儀表板或任何流程改讀 watch 分頁，
會直接漏掉第一順位的矽品。**一律以 `docs/data/watchlist.json` 為準。**

**待人工處理**：開 Apps Script 編輯器檢查 `doPost` 的 `action==='watch'` 分支
（`added` 計數與 `appendRow` 目標 sheet 是否對得上）。這已是第 7 次確認，建議優先修。

### 🔴 未解問題 2（事件紀錄分頁殘列）：**仍待人工刪除**

```
2026-08-19 2:48 | 聯詠 | 3034 |（其餘 9 欄全空）
```
聯詠正式事件列在 `2026-08-19 2:46`，刪掉 2:48 那列不會遺失資料。

### 🔴 未解問題 3（經營進度分頁「記錄者」欄全空）：**仍在**

本輪 `?list=progress` 回傳 **52 列 × 5 欄**，第 5 欄（記錄者）依然全部為空字串。
仍無法區分現傑（mac2good909777）與助理（yubob0513），回顧段落以動作類型推定分工。

### ℹ️ 本輪端點狀態：全部可直連

| 端點 | 結果 |
|---|---|
| `script.google.com`（GET `?list=watch/progress/records`、POST 事件列） | ✅ |
| `mopsov.twse.com.tw/mops/web/ezsearch_query`（11 組關鍵字＋3 家個股查詢） | ✅ 皆未達 1000 筆上限，無截斷 |
| `mopsov.twse.com.tw/mops/web/ajax_t05sr01_1`（重訊原文 ×3） | ✅ |
| 工業監控 C／D 線 `export_new_rows.py` | ✅ C 線 6 列、D 線 7 列 |
| WebSearch | ✅ |

---

## 2026-08-24 03:3x（雲端輪）：端點全數封鎖，**1 筆新事件待人工補 POST**

### 🔴 本輪端點狀態：與 02:4x 本機輪完全相反，全部不通

| 端點 | 結果 |
|---|---|
| `script.google.com`（`?list=` 讀 ＋ 事件 POST） | ❌ **403 CONNECT（egress policy denial）** |
| `openapi.twse.com.tw/v1/opendata/t187ap04_L` | ❌ 403 CONNECT |
| `mops.twse.com.tw` / `mopsov.twse.com.tw` / `doc.twse.com.tw` / `www.twse.com.tw` | ❌ 403 CONNECT |
| `www.tpex.org.tw` | ❌ 403 CONNECT |
| WebFetch（`house.ettoday.net` 等新聞站） | ❌ 多數 EGRESS_BLOCKED |
| **Google Drive MCP**（讀試算表 `1W_7ab…MBkmg`） | ✅ 改道成功，watch／progress／needs／records 四分頁全讀到 |
| **WebSearch** | ✅ |
| **GitHub** | ✅ |

**判讀**：這不是端點壞掉，是**執行環境差異**。同一天 02:4x 的「本機輪」全部端點皆通、
2 筆美光事件也 POST 成功；03:3x 的「雲端輪」在 Claude Code 遠端容器內執行，
受組織 egress policy 管制，`script.google.com` 與整組 TWSE 網域皆被擋在 CONNECT 階段。
**兩輪各有各的盲區，不要以為某一輪失敗就是資料源掛了。**

依 SKILL「script.google.com 封鎖時讀取改 Drive、寫失敗寫 repo」之規定，
本輪讀取已改道 Drive 完成，**寫入改記於此檔**。

### 📌 待人工補 POST：事件紀錄分頁 1 筆

| 欄位 | 值 |
|---|---|
| company | 鴻勁精密 |
| ticker | 7769 |
| eventDate | 2026-05-20 |
| eventType | 擴產設廠 |
| label | 擴張 |
| priority | A-立即接觸 |
| related | 臺中市政府經濟發展局（核准機關）；台積電中科1.4奈米新廠（需求源頭） |

summary／reading／entry／sources 全文已寫入 `docs/data/records.json` 最末一筆，
補 POST 時直接由該檔取用即可（**sources 已含 6 個完整 URL**）。

**性質**：回補（新聞先行，**待重訊確認**）。MOPS 7769 115/05/20 原文因本輪封鎖未取得，
下一次本機輪請補抓原文全文 blockquote 進 `docs/companies/鴻勁精密-7769.md`。

### 🔴 未解問題 1（`action=watch` 寫入無效）：本輪無法複驗

`script.google.com` 不通，**第 8 次驗證延到下一次本機輪**。
缺口維持 **16 家**（watch 分頁 30 家 vs `watchlist.json` 46 家）。
**一律以 `docs/data/watchlist.json` 為準**這條結論不變。

### 🔴 未解問題 2（事件紀錄分頁殘列）：仍待人工刪除

`2026-08-19 2:48 | 聯詠 | 3034 |`（其餘 9 欄全空）。本輪無法透過端點處理。

### 🔴 未解問題 3（經營進度分頁「記錄者」欄全空）：**本輪由 Drive 讀取再次確認仍空**

Drive 讀回的經營進度分頁共 5 欄，第 5 欄（記錄者）**全部為空字串**，
54 列進度紀錄無一例外。仍**無法區分現傑（mac2good909777）與助理（yubob0513）**，
本輪回顧一樣以動作類型推定分工。

**對照組**：同一份試算表的**需求描述分頁**，記錄者欄**是有值的**
（統一、信邦兩列皆為 `mac2good909777@gmail.com`）。
這代表問題**不在試算表本身，而在經營進度分頁的表單寫入端沒有帶入該欄**——
排查方向明確了，請檢查該分頁對應表單的欄位對應設定。

### 🟡 新問題 4（需求描述分頁資料品質）：3 列有 2 列有問題

| 公司 | 問題 |
|---|---|
| 信邦電子 | 預算 30,000 萬（3億）／地坪 2,000 坪／**建坪 8,000 坪**——3 億買不到這個量體（光建物造價就超過），且與公開的銅鑼租地委建 43 億、台中分公司 6.65 億都對不上。**欄位極可能誤填，供件前須確認** |
| 統一 | **方向／標的類型／交易方式三欄全空**，不知道要買還是要賣。由 3億÷1,200建坪＝每建坪約25萬反推，較可能是購置廠辦，但屬推論非事實 |
| 和大工業 | **整列僅有公司名稱**。但經營進度 8/10 已記「大里場要處分、已委託**專任**、不告知委託方」——需求描述沒登，導致每輪都要重新評估一次 |

**另有一筆口頭需求從未登錄**：聯純科技 8/20 由總務魏先生給出的完整規格
（700坪以上／工業區或精科區／一般倉儲／辦公空間要大），
目前**只存在經營進度的備註欄**，需求描述分頁沒有這家。
口頭需求不登錄＝換個人看就找不到，這是目前最該補的一列。

---

## 2026-08-28 02:4x（本機輪）：3 筆事件已寫入；`action=watch` **第 9 次確認**，但診斷有重大修正

### ✅ 事件紀錄分頁：3 筆全部 `{"ok":true}`

| 公司 | 代號 | 事件日 | 類型 |
|---|---|---|---|
| 大立光 | 3008 | 115/08/27 | 上下游動態（八方國際投資取得 Halo Industries 股份 1,700 萬美元／4.8%） |
| 南茂科技 | 8150 | 2026/08/11 | 擴產設廠（法說會宣布兩年高強度擴產，資本支出占營收比 20%→25%+） |
| 惠來產創 | 未上市 | 2026/08/27 | 其他（惠來產業園區彰化花壇 29 公頃、2026 營運、50 家廠商進駐） |

### 🔴 未解問題 1（`action=watch` 寫入無效）：**仍壞，但先前「自 8/08 後從未成功」的結論是錯的**

本輪為兩家新監控公司（**南茂科技 8150、惠來產創**）送出三種 payload，全部 `{"ok":true,"added":0}`、
`?list=watch` 讀回維持 34 列：

| payload 形狀 | 回應 | 是否落地 |
|---|---|---|
| `{action:"watch", company, ticker, tier, score, prob, kind, reason, advice, locs, next, nextKind, nextWhat, added, md}` | `{"ok":true,"added":0}` | ❌ |
| `{action:"watch", watch:[{company,ticker,reason,added}]}` | `{"ok":true,"added":0}` | ❌ |
| `{action:"watch", company, ticker, reason, added}`（最小四欄） | `{"ok":true,"added":0}` | ❌ |

**但本輪比對 `?list=watch` 的「加入日期」欄，推翻了 2026-08-24 那次的診斷：**

> 2026-08-24 記載「watch 分頁加入日期只有 2026-08-07 與 2026-08-08 兩天，
> 也就是 `action=watch` 自 2026-08-08 之後從未成功寫入過任何一列，不是偶發。」

**這句話現在不成立。** 本輪讀回的加入日期為 **`2026-08-07`／`2026-08-08`／`2026-08-25`／`2026-08-26`** 四天，
其中 8/25～8/26 的三列確實落地了：

| 公司 | 加入日期（儲存值） | 關注原因（前 60 字） |
|---|---|---|
| 科宏工業 | 2026-08-25T16:00:00.000Z | A級｜2026/08/25以30.8億元將精科一路8號、10號整廠…售予大立光＝處分方 |
| 科沃斯先進 | 2026-08-26T16:00:00.000Z | 2026/08/26 向法人股東穗曄實業承租彰化線西彰濱工業區慶安南二路18號… |
| 穗曄實業 | 2026-08-26T16:00:00.000Z | 2026/08/26 將彰化線西慶安南二路18號及18之1號『部分』土地建物出租予科沃斯先進… |

**修正後的診斷：這不是「handler 完全失效」，是「間歇性失效」。**
排除了 payload 欄位名（三種形狀都試過）、排除了字串長度（8/26 成功的三列 reason 也有 130 字以上）、
排除了網路（本輪端點全通、同一支腳本的事件 POST 三筆全成功）。

**建議的人工排查方向因此改變**：不要再從「欄位名對不對」找，
改查 `doPost` 的 `action==='watch'` 分支裡**取 sheet 物件與判重的那一段**——
間歇性通過比較像是「判重條件誤判為已存在」或「取到錯的 sheet 物件時靜默 return」，
而不是欄位解析錯誤。（參考先前 Result_D 欄位偏移的成因，同樣是沒釘 sheet／range。）

### 📌 目前缺口：watch 分頁 33 家 vs `watchlist.json` **51 家，缺 18 家**

| 公司 | 代號 | 級別/分數 |
|---|---|---|
| 大立光 | 3008 | A 96（**全名單第一順位**） |
| 矽品精密工業 | 3711 | A 95 |
| 崇越 | 5434 | A 85 |
| 台化 | 1326 | A 82 |
| 友達 | 2409 | A 74 |
| 美光台灣 | MU | A 70 |
| **南茂科技** | **8150** | **A 70（本輪新增）** |
| **惠來產創** | **未上市** | **A 55（本輪新增）** |
| 振鋒企業 | 4562 | B 45 |
| 南亞科技 | 2408 | B 45 |
| 廣達 | 2382 | C 38 |
| 巧新科技 | 1563 | C 35 |
| 永泉豐企業 | 未上市 | C 30 |
| 全新光電 | 2455 | C 30 |
| 聯詠 | 3034 | C 30 |
| 巨大 | 9921 | C 30 |
| 光罩 | 2338 | C 30 |
| 亞泰冷藏 | 未上市 | C 20 |

**一律以 `docs/data/watchlist.json` 為準。**

### 🔴 未解問題 2（事件紀錄分頁殘列）：仍待人工刪除

`2026-08-19 2:48 | 聯詠 | 3034 |`（其餘 9 欄全空）。正式列在 `2026-08-19 2:46`，刪掉不會遺失資料。

### 🔴 未解問題 3（經營進度分頁「記錄者」欄全空）：**仍在**

本輪 `?list=progress` 回傳 **66 列 × 5 欄**，第 5 欄（記錄者）**依然全部為空字串**。
仍無法依 SKILL 要求區分現傑（mac2good909777）與助理（yubob0513），
本輪回顧一樣以動作類型推定分工（電訪／寄信＝助理前端；委託／確認需求／帶看＝現傑）。

### 🟡 未解問題 4（需求描述分頁）：聯純科技口頭需求仍未登錄，已第 5 天

聯純科技 8/20 由總務魏先生給出的完整規格（700 坪以上／工業區或精科區／一般倉儲／辦公空間要大，
且**主動表示可約時間帶案件拜訪**）目前**只存在經營進度的備註欄**，需求描述分頁沒有這家。
這是全名單唯一一筆「對方主動邀約帶看」的需求，**不登錄＝換個人看就找不到**。

### ℹ️ 本輪端點狀態：全部可直連

| 端點 | 結果 |
|---|---|
| `script.google.com`（GET `?list=watch/progress`、POST 事件列 ×3） | ✅ |
| `mopsov.twse.com.tw/mops/web/ezsearch_query`（16 組關鍵字全市場 ＋ 41 家個股 14 天 ＋ 1536 區間查詢） | ✅ 皆未達 1000 筆上限，**無截斷** |
| `mopsov.twse.com.tw/mops/web/ajax_t05sr01_1`（重訊原文 ×3：2885、3008、1536） | ✅ |
| 工業監控 C／D 線 `export_new_rows.py` | ✅ C 線 17 列、D 線 signal-only 0 列／全量 10 列（確認 00:10 那輪有正常執行） |
| WebSearch | ✅ |
